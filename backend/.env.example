# Base de données PostgreSQL
# En développement local : postgresql://user:password@localhost:5432/tetrix_plus?schema=public
# En production : Utiliser la DATABASE_URL fournie par votre hébergeur (Render, Heroku, etc.)
DATABASE_URL="postgresql://user:password@localhost:5432/tetrix_plus?schema=public"

# JWT Secret (générer une clé sécurisée en production)
# IMPORTANT : Changez cette valeur en production avec une clé aléatoire de 64+ caractères
# Générer avec : node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET="votre-secret-jwt-ultra-securise-changez-moi-en-production"

# Configuration serveur
PORT=3001
NODE_ENV=development

# CORS - Frontend URL
# En développement : http://localhost:5173
# En production : https://votre-domaine.com
FRONTEND_URL=http://localhost:5173
